<h2>Editing Book</h2>

<% if flash[:notice] %>
<p class="flash">
	<%= flash[:notice] %>
</p>
<% end %>

<%= form_for(@book) do |f| %>

<% if @book.errors.any? %>
<h3><%= @book.errors.count %>errors prohibited this book from beiing updating</h3>

<ul>
	<% @book.errors.full_messages.each do |message| %>
	<li><%= message %></li>
	<% end %>
</ul>
<% end %>



	<h3>Title</h3>
<%= f.text_field :title %>

	<h3>Opinion</h3>
	<%= f.text_field :body %>

	<%= f.submit 'Update Book' %>

	<%= link_to "back", books_path %>
	<%= link_to "show", book_path(@book) %>

	<% end %>